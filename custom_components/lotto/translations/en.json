{
  "config": {
    "step": {
      "user": {
        "title": "Lotto",
        "description": "The API key is optional. Leave it blank to use the free, unofficial lotto.pl results feed right away. Enter a Lotto Open API key (developers.lotto.pl, request one by emailing openapi@totalizator.pl) to use the official API instead.",
        "data": {
          "api_key": "API key (Secret) — optional",
          "poll_interval_hours": "How often to check results (hours)"
        }
      }
    },
    "error": {
      "invalid_auth": "The API key was rejected.",
      "cannot_connect": "Could not connect to the results provider."
    },
    "abort": {
      "already_configured": "Lotto is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Lotto settings",
        "data": {
          "api_key": "API key (Secret) — optional",
          "poll_interval_hours": "How often to check results (hours)"
        }
      }
    },
    "error": {
      "invalid_auth": "The API key was rejected.",
      "cannot_connect": "Could not connect to the results provider."
    }
  }
}
